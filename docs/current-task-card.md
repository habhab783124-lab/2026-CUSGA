# 当前任务卡
Status: 进行中
Updated: 2026-05-19

## 当前任务
- 把本地 `main` 的最新内容合并到当前分支 `构建工作流`，让当前工作区也同步到新的主线基线。

## 本次只做
- 检查当前分支与 `main` 的差异
- 更新任务卡到本次“合并 main 到构建工作流”动作
- 通过任务门禁脚本检查
- 处理会阻挡合并的本地任务卡改动
- 执行 `main -> 构建工作流` 合并
- 如果出现冲突，做最小范围处理
- 最后确认当前分支已经包含最新 `main`

## 本次明确不做
- 不改 `main` 分支本身
- 不做变基，不改写当前已推送分支历史
- 不清理 `.codex-temp-*`、`.dotnet/`、`*.lscache` 之类本地临时产物
- 不额外重构玩法、场景、预制体、脚本或资源

## 完成标准
- `构建工作流` 成功合并最新 `main`
- Unity 当前工作区文件随之拿到 `main` 的更新
- 如果有冲突，已经被明确处理并收口
- 本地临时文件没有被误提交或误删除

## 直接相关文件
- `docs/current-task-card.md`
- `docs/current-task-card.json`
- `.git` 分支历史与合并记录
- 合并过程涉及的冲突文件（如果有）

## 任务来源
- 本轮任务直接来源于：用户在得知“编辑器里没变化是因为当前仍在 `构建工作流`”后，明确要求把 `main` 的最新内容“合并”到当前分支并回复“执行”
